# Java基础

## 第1题：JDK和JRE有什么区别？

JRE是Java运行环境(Java Runtime Environment)，包含了JVM和Java核心类库。

JDK是Java开发工具包(Java Developer's Kit)，包含了JRE和Java常见的开发工具(javac、java、jconsole、 jvisualvm...)。

## 第2题：&和&&有什么区别

&： 按位与

如果左边两边是数值，将两个操作数的二进制对齐后，1 & 1 为1，其余都为0。 如果左右两边是boolean值，true底层为1，false底层为0。只有两边为true，结果才为true。当左边 为false时，右边继续计算。

&&：短路与

左右两边必须是boolean值，只有两边为true，结果才为true。当左边为false时，右边表达式不计 算。

## 第3题：return、continue、break的区别

continue break return

用在哪循环体或switch-case结构 循环体中 方法体中 里 中

跳过本次循环剩下的语句，直接准备下结束本层循环或switch-结束当前 作用 一次循环case结构方法

## 第4题：说一下你对==和equals的认识，它们有什么差别？

## (高频)

1、==可以比较引用数据类型也可以比较基本数据类型：

比较基本数据类型，比较的是数值 比较引用数据类型，比较的地址值 Student s1 = new Student() ; Student s2 = new Student() ; sout(s1 == s2)

2、equals只能比较引用数据类型，是因为equals是Object类中的一个方法，要使用方法只能通过对象进 行使用，而对象就是引用数据类型：

1默认情况下比较的也是对象的地址值

可以通过重写Object类中的equals方法实现比较两个对象的属性

## 第5题：如果两个对象的hashCode值一样，则它们用

## equals()比较也是为true，是不是？(高频)

不是。

hashCode方法和equals方法是两个独立的方法，本质上没有任何的关系。它们都是Object类中的方法， 这两个方法可以被子类进行重写，子类重写可以自定义具体的逻辑。

## 第6题：int和Integer、BigInteger有什么区别？

int Integer BigInteger

基本数据类型包装类型（Wrapper 类不可变的任意精度整数类，位于 （PrimitiveClass），位于 java.lang 型java.math 包 Type）包

范围是从 范范围是从 -2,147,483,648 到 -2,147,483,648 到理论上无限制，取决于可用内存 围2,147,483,647 2,147,483,647

适用于大多数整用于将 int 转换为对象。 用于处理超出 int 和 long 范围 数运算，性能较提供了许多有用的静态方 适的大整数。 提供了许多数学运算方 高，因为它是基法，如 parseInt 、 用法，如加法、减法、乘法、除法、 本类型，不需要valueOf 等。 支持空值 幂运算等。 支持高精度计算。 对象开销（ null ）

类似的：double，Double，BigDecimal的区别

## 第7题：String是基本数据类型吗？

String不是基本数据类型，基础类型有8种：byte、boolean、char、short、int、float、long、double。 String属于引用数据类型！

## 第8题：对字符串操作都有哪些类并详细的介绍一下它们之

## 间的区别？(高频)

对字符串操作的类有：String , StringBuilder , StringBuffer

1、String是不可变的字符串

1String a = "abc" ；

2a = "bcd" ；

4不可变指的是字符串常量"abc"不可变,作为字符串的变量(a)是可以变的。

2、StringBuilder , StringBuffer是可变的字符串

StringBuilder , StringBuffer之间的区别：StringBuffer是线程安全的，StringBuilder是线程不安全的。

StringBuffer保证线程安全的原理：就是针对每一个方法都添加了一个synchronized关键字，将其声明为了 同步方法。

## 第9题：String str="abc"与 String str = new String("abc")的

## 定义方法一样吗？

不一样。

String str = "abc" ; 是创建一个字符串对象abc放入常量池中。

String str = new String("abc"); 创建了两个对象，"abc"字符串对象在常量池中，new String("")在堆内存中 又创建了一个新的对象。

## 第10题：String类isEmpty和isBlank的区别？

isEmpty用于判断字符串是不是为空，这里的空是指长度为0的字符串，即不包含任何字符的字符 串。 isBlank用于判断字符串是不是为空，这里的空是指字符串长度为0或全部由空白字符（例如空格、 \t、\n等）构成的字符串。

1public class TestStringEmptyBlank { 2public static void main(String[] args) { 3String str1 ="";

4String str2 =" \t\n "; 5System.out.println("字符串1[" + str1 +"]，isEmpty：" + str1.isEmpty() + "， isBlank：" + str1.isBlank()); 6System.out.println("字符串2[" + str2 +"]，isEmpty：" + str2.isEmpty() + "， isBlank：" + str2.isBlank()); 7} 8}

## 第11题：String底层实现是怎样的？

JDK9之前，String底层是char[]，每一个字符占2个字节，采用UTF16编码方式。

JDK9之后，String底层是byte[]，每一个字符可能占1个字节或2个字节。

如果当前字符串所有字符都是Latin1字符集中的字符（这些字符覆盖了大多数西欧语言，如英 语、法语、德语、西班牙语等所需的字母和符号，编码值范围是[0-255]），那么当前字符串每 一个字符占1个字节，采用Latin1编码方式。

如果当前字符串中有任意一个字符不是Latin1字符集中的字符，

例如中文、韩文、日文等，那么每一个字符占2个字节，采用UTF16编码方式。 例如：emoji表情字符等，那么每一个字符占4个字节，采用UTF16编码方式。

## 第12题：普通for和增强for的区别

作用与应用场景：

普通for循环，可以用于所有需要重复执行某些语句的场景。 增强for循环，只能用于遍历数组与Collection集合。

使用区别：

如果使用普通for遍历数组，需要指定下标值，可以修改数组的元素。 如果使用增强for遍历数组，不需要指定下标，但无法修改数组的元素。 另外，增强for循环只是一种语法糖，增强for循环遍历数组时编译器仍然会将对应代码转换为普通 for循环；增强for循环遍历Collection集合时编译器会将对应代码转换为Iterator迭代器遍历集合的代 码。

## 第13题：方法重载与重写的区别

## 第14题：综合说下final的作用？

修饰在变量上，叫常量，该常量必须初始化，初始化之后值就不能被修改，而常量一般全都是用大 写来命名。 修饰在类上，该类不能被继承。 修饰在方法上，该方法不能被重写。

## 第15题：抽象类与接口的区别

在面向对象编程中，特别是在Java语言中，抽象类（Abstract Class）和接口（Interface）都是用来提供一 种形式的抽象和代码复用的方式。尽管它们有相似之处，但它们在设计意图和实现细节上有显著的区 别。以下是抽象类和接口的一些关键区别：

抽象类

1. 继承限制：有单继承限制。

2. 成员：

抽象类可以有多种类型的成员变量，包括实例变量和静态变量。 抽象类可以有构造方法，这有助于子类的初始化。 抽象类可以包含初始化块（静态代码块和非静态代码块）。 抽象类可以包含抽象方法和普通方法。

接口

1. 多实现：一个类可以实现多个接口

2. 成员：

接口中不允许有构造方法和初始化块（静态代码块和非静态代码块） 接口成员变量默认是 public 、 static 和 final 的，即常量。 接口的接口中的方法默认是 public 的。JDK9之后允许有private方法。 Java8之前接口中的所有方法默认是抽象的，在Java 8之后可以包含默认方法和静态方法。

设计意图

抽象类通常用来定义一组相关类的公共行为和状态，以及一些通用的功能实现。 接口则更多地用来定义类的行为规范，而不关心具体的实现细节。它通常用来表达“这个类能做什 么”。

## 第16题：谈谈你对面向对象三大特性的理解

面向对象编程（OOP）的三大核心特性分别是封装、继承和多态。这些特性共同构成了面向对象设计的 基础，使代码更具模块性、可重用性和可维护性。下面分别解释这三个特性：

1. 封装（Encapsulation）： 封装是指将数据（属性）和操作这些数据的方法（行为）捆绑在一起， 形成一个整体，也就是“对象”。同时，封装还意味着隐藏对象的内部状态和实现细节，只暴露必 要的公共接口供外部访问。这样做的好处是可以保护对象的状态不被外部代码随意更改，从而增 加了代码的安全性和稳定性。封装还使得对象的实现可以独立于使用对象的代码，提高了代码的 可维护性。

在Java中，封装主要通过访问控制符（如 private 、 protected 和 public ）来实现， private 关键字可以限制只有类内部的代码可以访问某些属性和方法，而 public 则可以让外部 代码访问。此外，还可以通过提供getter和setter方法来间接访问和修改属性，以增加对属性的控 制。

2. 继承（Inheritance）： 继承允许创建一个新类（子类），该类继承了现有类（父类）的属性和方 法。子类可以复用父类的代码，同时也可以覆盖或扩展父类的行为。继承支持代码的重用，减少 了代码重复，使得程序结构更加清晰和模块化。

在Java中，继承通过 extends 关键字实现。

3. 多态（Polymorphism）： 多态是指同一种事物在不同情况下有不同的表现形式。多态有两种形 式：静态多态（也称为编译时多态，如方法重载）和动态多态（也称为运行时多态，如方法重 写）。动态多态是通过继承和接口实现的，允许我们使用父类或接口类型的引用指向子类对象， 然后调用方法时，会根据对象的实际类型来决定调用哪个方法的实现。这样可以编写出更加灵活 和可扩展的代码。

## 第17题：访问修饰符的作用范围由大到小，及各自的范围是

## 什么？可以 修饰什么？

修饰符的作用范围由大到小:public、protected、缺省、private

本类 本包的所有类 其他包的子类 其他包的非子类

private √ × × ×

缺省（不写） √ √ × ×

protected √ √ √ ×

public √ √ √ √

这些权限修饰符可以修饰的角色：

class（外部类） 成员变量 成员方法 成员内部类

private × √ √ √

缺省（不写） √ √ √ √

protected × √ √ √

public √ √ √ √

## 第18题：throw与throws的区别

在Java编程语言中， throw 和 throws 都与异常处理相关，但它们的用途和语法有着明显的区别：

1. throw

throw 是一个关键字，用于在代码中显式地抛出一个异常。 它后面跟着一个异常对象，这个对象通常是某个异常类的实例。 throw 通常出现在方法体内部，当特定条件满足时执行。 使用 throw 抛出异常会立即终止当前方法的执行，并将控制权转移到调用栈上的上一层， 寻找适当的异常处理器（如 catch 块）。
2. throws

throws 也是一个关键字，但它用于方法的声明部分，而不是方法体内部。 它后面跟着一个或多个异常类的名称，这些异常是方法可能抛出的，用逗号分隔。 当一个方法不能或不应该处理它内部发生的某些异常时，可以使用 throws 来声明这些异 常，这样调用者必须处理这些异常，要么捕获处理，要么继续声明抛出。 throws 提供了对调用者的一种合同，表明方法可能抛出哪些类型的异常，使调用者能够 做出相应的准备。

总结来说：

throw 用于抛出具体的异常对象，它意味着一个异常正在发生。 throws 用于声明方法可能抛出的异常类型，它意味着如果在方法内部发生这些异常，方法本身不 会处理它们，而是将它们抛给调用者。

## 第19题：Java的集合容器都有哪些？(高频)

## 第20题：Collection和Collections有什么区别？

Collection 是一个单列集合的顶层接口。

Collections是集合类的一个工具类，包含了对集合元素进行排序和线程安全等各种操作方法。

## 第21题：如何把一个线程不安全的List集合转成线程安全的

## List集合？

方案一：直接使用Collections集合工具类的 synchronizedList() 方法

方案二：直接选择 Vector 或 CopyOnWriteArrayList 。

方案三：自定义一个类，让其去实现List接口，重写List接口中的方法，然后在该方法加同步保证线程安 全性。

## 第22题：List、Set、Map 之间的区别是什么？(高频)

List Set Map

单列or双单列集 单列集合 双列集合，存储键值对 列合

索引 支持 × ×

重复 重复 × key不重复，value可重复

存取一致LinkedHashSet一致，其余不一LinkedHashMap一致，其余不一 一致 性 致致

## 第23题：HashMap 和 Hashtable 有什么区别？(高频)

不同点：

1、HashMap可以把null作为key或value，Hashtable不可以

2、HashMap线程不安全，效率高。Hashtable线程安全，效率低。

## 第24题：如何决定使用 HashMap 还是 TreeMap？

TreeMap可以对元素进行排序，因此如果需要对元素按照某一种规则进行排序，此时可以选择TreeMap。 没有特殊要求一般选择HashMap。

## 第25题：ArrayList 和 LinkedList 的区别是什么？

ArrayList底层实现的数据结构是数组，根据下标查询效率极高，时间复杂度是O(1)，非末尾位置增删需要 移动元素，添加元素需要扩容！

LinkedList 的底层数据结构是双向链表，根据下标查询效率低，时间复杂度是O(n)，增删元素时，不需要 扩容和移动元素，只需要修改前后结点的引用关系！

理论上数组结构的增删慢，链表结构的增删快，但现在内存拷贝技术性能提高之后，ArrayList 的整体性 能反而优于 LinkedList，因为 LinkedList 内部需要创建结点对象，这是一个耗时耗力的过程。

附某次测试结果如下：

1ArrayList头部插入100000个元素耗时：402 2LinkedList头部插入100000个元素耗时：6

4ArrayList尾部插入100000个元素耗时：6 5LinkedList尾部插入100000个元素耗时：5

7ArrayList任意位置插入100000个元素耗时：198 8LinkedList任意位置插入100000个元素耗时：14393

10ArrayList删除头元素100000个耗时：418 11LinkedList删除头元素100000个耗时：2

13ArrayList删除尾元素100000个耗时：2 14LinkedList删除尾元素100000个耗时：2

16ArrayList删除任意位置元素100000个耗时：187

17LinkedList删除任意位置元素100000个耗时：3914

## 第26题：如何做到数组和List之间的转换？

List对象转换成为数组：可以调用List的toArray方法。

数组转换成为List：调用Arrays的asList方法，但该方法得到的是一个不可增删元素的List集合。

## 第27题：ArrayList 和 Vector 的区别是什么？

ArrayList和Vector都是List接口的实现类，它们都是动态数组。

Vector ArrayList

版本 JDK1.0（最古老） JDK1.2（较新）

线程安全 安全 不安全

0， 默认初始化容10，直接创建长度为10的先初始化为 量数组DEFAULTCAPACITY_EMPTY_ELEMENTDATA， 然后在首次add元素时才创建长度为10的数组

扩容 2倍 1.5倍

扩容频率低，空间利用率 扩容频率高，空间利用率更高 低

相关的面试题：

问：什么是Vector？它是List接口的实现类，是古老的动态数组。

问：ArrayList默认大小是多少，是如何扩容的？见上表

问：ArrayList是线程安全的吗？不是

问：ArrayList为什么不是线程安全的？ArrayList 没有内置的同步机制来确保多线程环境下的操作安 全。这意味着在多线程环境下使用 ArrayList 时，需要外部手动添加同步机制，如使用 synchronized 关键字或 Collections.synchronizedList 方法。

## 第28题：如何遍历List集合

List遍历有4种方式：

普通for循环 foreach Iterator ListIterator

1import org.junit.Test;

3import java.util.ArrayList; 4import java.util.Iterator; 5import java.util.ListIterator;

7public class TestListIterate { 8@Test 9public void test1(){

10ArrayList<String> list = new ArrayList<>(); 11list.add("尚硅谷"); 12list.add("atguigu"); 13//使用普通for循环遍历 14for (int i = 0; i < list.size(); i++) { 15String s = list.get(i); 16System.out.println(s); 17} 18} 19@Test 20public void test2(){

21ArrayList<String> list = new ArrayList<>(); 22list.add("尚硅谷"); 23list.add("atguigu");

24//使用增强for循环遍历 25for (String s : list) { 26System.out.println(s); 27} 28}

29@Test 30public void test3(){ 31ArrayList<String> list = new ArrayList<>(); 32list.add("尚硅谷"); 33list.add("atguigu"); 34//使用Iterator迭代器遍历 35Iterator<String> iterator = list.iterator(); 36while(iterator.hasNext()){ 37String s = iterator.next(); 38System.out.println(s); 39}

40}

42@Test

43public void test4(){ 44ArrayList<String> list = new ArrayList<>(); 45list.add("尚硅谷"); 46list.add("atguigu"); 47//使用ListIterator迭代器遍历 48ListIterator<String> iterator = list.listIterator(); 49System.out.println("从前往后遍历："); 50while(iterator.hasNext()){ 51String s = iterator.next(); 52System.out.println(s); 53}

54System.out.println("从前往后遍历："); 55while(iterator.hasPrevious()){ 56String s = iterator.previous();

57System.out.println(s);

58} 59} 60}

## 第29题：哪些集合类是线程安全的？

Vector：比Arraylist多了个同步化机制（线程安全）

Stack ：堆栈类，是Vector的子类，先进后出，项目中用得并不多。

Hashtable：比Hashmap多了个线程安全，项目中一般使用HashMap。

ConcurrentHashMap：是线程安全的，并且比Hashtable效率高。

## 第30题：栈和队列有什么区别？

栈：先进后出（FILO：First In Last Out）。队列：先进先出（FIFO：First In First Out）。

拓展：

栈和队列只是逻辑结构，其物理结构可以是数组，也可以是链表。

核心类库中的栈结构有Stack和LinkdeList。Stack就是顺序栈，它是Vector的子类。LinkedList是链式栈。

操作 方法

入栈 push(e)

出栈 pop(e)

查看栈顶元素 peek()

Queue 除了基本的 Collection 操作外， 队列 还提供其他的插入、提取和检查操作。每个方法都存 在两种形式：一种抛出异常（操作失败时），另一种返回一个特殊值（ null 或 false ，具体取决于 操作）。 Queue 实现通常不允许插入 元素，尽管某些实现（如 ）并不禁止插入 。即使在允许 null 的实 现中，也不应该将null插入到中，因为null也用作 方法的一个特殊返回值，表明队列不包含元素，此时不 好区分。

抛出异常返回特殊值

插入 add(e) offer(e)

移除 remove() poll()

检查 element() peek()

Deque ，名称deque 是“double ended queue （双端队列） ”的缩写，通常读为“deck”。此接口定义在 双端队列两端访问元素的方法。提供插入、移除和检查元素的方法。每种方法都存在两种形式：一种形 式在操作失败时抛出异常，另一种形式返回一个特殊值（ null 或 false ，具体取决于操作）。Deque 接口的实现类有ArrayDeque和LinkedList，它们一个底层是使用数组实现，一个使用双向链表实现。

第一个元素（头部） 最后一个元素（尾部）

抛出异常特殊值抛出异常特殊值

插入 addFirst(e) offerFirst(e) addLast(e) offerLast(e)

移除 removeFirst() pollFirst() removeLast() pollLast()

检查 getFirst() peekFirst() getLast() peekLast()

## 第31题：几种Set集合的区别？

HashSet LinkedHashSet TreeSet

底层集合 HashMap LinkedHashMap TreeMap

数据结构 数组+单链表+红黑树 数组+单链表+红黑树+双链表 红黑树

顺序特点 无序 插入顺序 按元素大小顺序

key支持null √ √ ×

线程安全 不安全 不安全 不安全

## 第32题：JDK1.7到JDK1.8，HashMap底层发生了什么变化？

## (高频)

1.8 之后 HashMap 的数据结构发生了变化，从之前的单纯的数组+单链表结构变成数组 + 单链表 + 红黑 树。

在HashMap 中存储 K-V 时仅通过 key 来决定每一个 entry 的存储槽位（Node[]中的 index）。多个K-V键值 对的index相同，就以链表的形式挂到对应槽位上。

如果只有链表结构，链表长度越长，数据查询的效率越低，链表查询的时间复杂度O(n）。为了提高数据 的查询效率，在JDK1.8中就引入了红黑树， 当数组的长度大于64并且某个槽位index下链表长度超过8的时 候，此时就会把链表变成红黑树， 红黑树查询的时间复杂度是O(logn)。

## 第33题：HashMap的数组长度有什么特点，扩容原理是什

## 么？（高频）

1、HashMap的默认的初始化的容量：16，也可以手动指定数组长度，数组长度一定是2的n次方，如果指 定的长度不是2的n次方，会被纠正为大于该值的最近的2的n次方。这是因为K-V键值对存储到HashMap中 时，会通过key 来决定每一个 entry 的存储槽位index，index=（数组的长度-1）& key的hash值，此时数组 的长度是2的n次方值，才能确保每一个Node[]数组的位置都有均等的机会。

2、当HashMap中的元素size值，超过了HashMap的阈值时进行扩容，每次扩容2倍。阈值的计算 = 最大 容量 * 负载因子(0.75)

第一次扩容： 阈值12 ，扩容完毕以后最大容量：32

第二次扩容： 阈值24 ， 扩容完毕以后最大容量：64

依次类推。

3、还有一个场景也会导致扩容，就是当Node[]数组某一个index位置下的K-V数量达到8，但是数组长度还 未达到64时，此时往该index位置继续添加新K-V，就会导致数组扩容。

## 第34题：Lambda表达式是什么？有什么用？

Lambda表达式其实就是匿名函数，它是实现SAM接口（Single Abstract Method，函数式接口）的一种语 法糖，它使得Java也成为支持函数式编程的语言。Lambda写的好可以极大的减少代码冗余，同时可读性 也好过冗长的（啰嗦的）匿名内部类。

Lambda表达式可以用于给函数式接口的变量赋值。例如：当某个方法的参数或返回值类型是函数式接口 类型时，Lambda表达式可以被用作参数传递或者返回值。

## 第35题：线程的创建方式？(高频)

JavaSE阶段面试答前两种即可，工作面试答四种。（后两种在JUC部分讲解）

① 继承Thread类

1public class MyThread extends Thread {

3@Override 4public void run() {

5System.out.println("MyThread...run..."); 6}

9public static void main(String[] args) {

11// 创建MyThread对象 12MyThread t1 = new MyThread() ; 13MyThread t2 = new MyThread() ;

15// 调用start方法启动线程 16t1.start(); 17t2.start();

19}

21}

② 实现Runnable接口

1public class MyRunnable implements Runnable {

3@Override 4public void run() { 5System.out.println("MyRunnable...run..."); 6}

8public static void main(String[] args) {

10// 创建MyRunnable对象

11MyRunnable mr = new MyRunnable() ;

13// 创建Thread对象

14Thread t1 = new Thread(mr) ; 15Thread t2 = new Thread(mr) ;

17// 调用start方法启动线程 18t1.start(); 19t2.start();

21}

23}

③ 实现Callable接口

1public class MyCallable implements Callable<String> {

3@Override 4public String call() throws Exception { 5System.out.println("MyCallable...call...");

6return "OK"; 7}

9public static void main(String[] args) throws ExecutionException, InterruptedException {

11// 创建MyCallable对象 12MyCallable mc = new MyCallable() ;

14// 创建F 15FutureTask<String> ft = new FutureTask<String>(mc) ;

17// 创建Thread对象 18Thread t1 = new Thread(ft) ;

19Thread t2 = new Thread(ft) ;

21// 调用start方法启动线程

22t1.start();

24// 调用ft的get方法获取执行结果 25String result = ft.get();

27// 输出 28System.out.println(result);

30}

32}

④ 线程池创建线程

1public class MyRunnable implements Runnable{

3@Override 4public void run() { 5System.out.println("MyRunnable...run..."); 6}

8public static void main(String[] args) {

10// 创建线程池对象 11ExecutorService threadPool = Executors.newFixedThreadPool(3); 12threadPool.submit(new MyRunnable()) ;

14// 关闭线程池 15threadPool.shutdown();

17}

19}

## 第36题：线程的run()和start()有什么区别？(高频)

start方法：启动线程，start方法只能调用一次，调用start方法以后，线程的状态就变成RUNNABLE状态 (可运行的状态)，当线程获取到了CPU的执行权以后，就开始执行run方法中所封装代码。

run方法：封装要被线程执行的代码，一个线程的run方法执行结束，就表示线程的生命周期结束了。

如果直接调用run方法而非start方法，那么此时线程对象只是一个普通Java对象，程序仍然是单线程执行 模式，并未开启多线程执行模式。

## 第37题：如何停止一个正在运行的线程？(高频)

① 使用退出标志，使线程正常退出。

1public class MyThread extends Thread {

3volatile boolean flag = false ; // 线程执行的退出标记

5@Override 6public void run() { 7while(!flag) { 8System.out.println("MyThread...run...");

9try { 10Thread.sleep(3000); 11} catch (InterruptedException e) {

12e.printStackTrace(); 13} 14} 15}

17public static void main(String[] args) throws InterruptedException {

19// 创建MyThread对象 20MyThread t1 = new MyThread() ; 21t1.start();

23// 主线程休眠6秒 24Thread.sleep(6000);

26// 更改标记为true 27t1.flag = true ;

29} 30}

② 使用stop方法强行终止，但是stop方法已过时

1public class MyThread extends Thread {

3@Override 4public void run() { 5while(true) { 6System.out.println("MyThread...run..."); 7try {

8Thread.sleep(3000); 9} catch (InterruptedException e) { 10e.printStackTrace();

11} 12} 13}

15public static void main(String[] args) throws InterruptedException {

17// 创建MyThread对象 18MyThread t1 = new MyThread() ; 19t1.start();

21// 主线程休眠2秒

22Thread.sleep(6000);

24// 调用stop方法

25t1.stop(); 26} 27}

## 第38题：单例设计模式（高频）

某个类的对象在整个系统运行期间有且只有1个，这样的类被称为单例类。单例类的通用写法与套路被称 为单例设计模式。

通常单例类的写法分为饿汉式与懒汉式。

饿汉式：在类初始化时创建单例对象。

懒汉式：在首次获取这个单例对象时，创建它。

饿汉式写法一：

1public enum SingleOne { 2INSTANCE 3}

饿汉式写法二：

1public class SingleTwo { 2public static final SingleTwo INSTANCE = new SingleTwo(); 3private SingleTwo(){

5} 6}

饿汉式写法三：

1public class SingleThree { 2private static final SingleThree instance = new SingleThree(); 3private SingleThree(){

5}

7public static SingleThree getInstance(){ 8return instance;

9} 10}

懒汉式写法一：

1public class SingleFour { 2private static SingleFour instance;//这里不new

4private SingleFour(){ 5}

7//当外面调用getInstance()时，说明确实需要这个类的对象，此时再new 8public static synchronized SingleFour getInstance(){ 9if(instance == null) { 10instance = new SingleFour();

11} 12return instance; 13} 14}

1public class SingleFour { 2private static SingleFour instance;//这里不new

4private SingleFour(){ 5}

7public static SingleFour getInstance(){ 8if(instance == null) { 9synchronized (SingleFour.class) {

10if (instance == null) { 11instance = new SingleFour(); 12}

13} 14} 15return instance; 16} 17}

懒汉式写法二：

1public class SingleFive { 2private SingleFive(){ 3}

5private static class Inner{ 6static SingleFive instance = new SingleFive(); 7}

9//Inner类的加载和初始化，是在调用getInstance方法时，用到Inner类，才初始化，才new了

SingleFive的对象 10public static SingleFive getInstance(){ 11return Inner.instance;

12} 13}

## 第39题：Java 中 IO 流分为几种？

按功能来分可以分输入流（input）和输出流（output）。

从类型来分可以是字节流和字符流。

把两种方式结合起来就会形成四种流：

1、字节输入流 ----> InputStream，例如：FileInputStream，BufferedInputStream，ObjectInputStream

2、字节输出流 ----> OutputStream，例如：FileOutputStream，BufferedOutputStream， ObjectOutputStream

3、字符输入流 ----> Reader，例如：FileReader，BufferedReader，InputStreamReader

4、字符输出流 ----> Writer，例如：FileWriter，BufferedWriter， OutputStreamWriter

## 第40题：Java序列化是什么？

序列化和反序列化是Java中实现 对象 持久化的重要机制。

序列化是指将对象的状态信息转换为字节流的过程，以便于将这些字节流保存到文件、数据库或通 过网络传输。 反序列化则是将字节流恢复为对象的过程。

要使一个对象可序列化，需要实现 Serializable 接口。Serializable 接口是一个标记接口，没有任何方法需 要实现。实现 Serializable 接口需要在类中添加一个常量字段：

1private static final long serialVersionUID = 值;

默认类中static、transient修饰的字段不参与序列化。

## 第41题：TCP和UDP协议有什么区别？

TCP（传输控制协议，Transmission Control Protocol）和 UDP（用户数据报协议User Datagram Protocol） 是两种常用的传输层协议，它们在功能和特性上有显著的区别。以下是TCP和UDP的主要区别：

1. 连接方式 TCP：面向连接的协议。在数据传输之前，需要先建立连接（三次握手），数据传输完 成后需要断开连接（四次挥手）。 UDP：无连接的协议。数据传输前不需要建立连接，直接发送 数据报文。
2. 可靠性 TCP：提供可靠的数据传输服务。通过确认应答、重传机制、拥塞控制等手段确保数据的 完整性和顺序性。 UDP：不保证数据的可靠传输。数据报文可能丢失、重复或乱序，但不会进行 重传或排序。
3. 数据传输方式 TCP：基于字节流的传输方式。数据被分割成多个段（segments），每个段都有序 列号，接收端根据序列号重新组装数据。 UDP：基于数据报的传输方式。数据被封装成一个个独 立的数据报文（datagrams），每个数据报文独立传输，没有顺序保证。
4. 速度和效率 TCP：由于可靠性高，需要进行多次握手、确认应答等操作，因此传输速度相对较 慢。 UDP：由于没有连接建立和确认应答等操作，传输速度快，效率高。
5. 流量控制和拥塞控制 TCP：提供流量控制和拥塞控制机制。通过滑动窗口机制调节发送速率，避 免网络拥塞。 UDP：不提供流量控制和拥塞控制机制。发送方可以以最大速率发送数据，可能导 致网络拥塞。
6. 报头大小 TCP：报头较大，标准TCP报头至少20字节。 UDP：报头较小，标准UDP报头只有8字 节。
7. 适用场景 TCP：适用于需要可靠传输的场景，如文件传输、电子邮件、网页浏览等。 UDP：适用 于对实时性要求高、对数据丢失容忍度高的场景，如视频直播、语音通话、在线游戏等。

## 第42题：你都了解哪些AI插件或AI工具，可以帮助程序员高

## 效开发？

本题是开放题，答案不限于下面的参考答案，选择其中你知道的回答即可。

以下是几款主流的 AI 代码生成插件（支持 JetBrains IDE/VS Code 等），从技术、功能、适用场景等维度 进行对比分析，供参考：

开发公核心技术模 工具名称 核心优势 适用场景 定价 司型

$10/月

- 多语言支持广全栈开发、（个 GitHubOpenAI GPT-（Python/JS/TS等） - 深度集开源项目、人） GitHub Copilot (微软)4 Turbo成 GitHub 生态 - 智能代码补企业级$19/月

全与测试生成DevOps（企

业）

AWS 云原生 自研模型- 完全免费 - 优化 AWS 服务 Amazon开发、 AWS（基于 AWS开发（Lambda/S3等） - 代码免费 CodeWhisperer Python/Java 代码库）安全扫描 项目

个人开发免费
- 完全免费（个人版） - 支持 Codeium自研模型者、多语言（企业 Codeium 70+ 语言 - 内置代码搜索 & 聊 Inc.（Cascade）项目、快速版收 天功能 原型费）

免费版

- 企业级隐私（支持本地部金融/军工+ 企业 Tabnine自研模型 + Tabnine 署） - 适应团队代码风格 -等高安全需版（定 Inc.StarCoder 30%+ 代码自动化生成求开发制定 价）

免费
- 中文优化 - 支持 Java/微服国内开发 通义千问（企业 通义灵码 阿里云 务/云原生 - 智能体模式者、阿里云 （Qwen3）版收 （@workspace）生态项目 费）

- 适配 昇腾芯片 - 支持硬件/政企/国产 CodeArts Snap 华为 盘古大模型物联网开发 - 企业级安全合化项目、嵌免费

规入式开发

文心大模型- 中文语义理解强 - 低代码生国内 Web/AI Comate 百度（ERNIE成优化 - 适合教育/文档密集开发、教学免费

Code）型项目场景

豆包1.5-pro- 轻量级 AI 插件 - 适合快速个人开发 字节跳 MarsCode + DeepSeek-补全与调试 - 字节生态集成者、小程免费 动 V3（如飞书）序/H5 开发

免费
- 微信生态优化（小程腾讯云开发 Tencent Cloud（企业 腾讯 混元 Turbo S序/H5） - 腾讯云深度集成 -者、游戏后 CodeBuddy 版收 企业研效看板端开发 费）

以下是几款与 Trae（字节跳动的 AI 原生 IDE）同类型的 AI 开发工具，它们均采用 AI 原生设计，支持自 然语言编程、智能代码生成和全流程开发辅助：

工具名称 开发公司 AI 模型 核心优势 适用场景 定价

Claude 3.5全栈开
- 项目级代码理解 Sonnet、GPT-发、复杂$20/月 （@Codebase） - Cursor Anysphere4o、DeepSeek-项目重（Pro Composer 多文件编辑 V3（可自定构、企业版）
- 智能预测 & 自动修复 义）级应用

中小型项
- Agent 模式（多步任 Cascade（自目、团队$15/月 务执行） - 上下文固定 Windsurf Codeium研）、Claude协作、快（Pro （Context Pinning） - 3.5、GPT-4o速迭代开版） 新手友好 UI 发

- 浏览器内全栈开发 -Web 开$20/月 自研模型（未 Bolt.new StackBlitz 对话式 AI 编程 - 云端发、快速（Pro 公开） 协作原型验证版）

GitHub 生 GPT-4 Turbo、- GitHub 深度集成 -$10/月 GitHub态、企业 Copilot++ Claude 3.5（可CI/CD 自动化 - 企业级（个人 （微软）DevOps 选）安全版） 流程

支持 Llama Continue金融/军免费 3、 团队（国- 完全离线运行 - 隐私工等高安（需本 ContinueDeepSeek、 际开源社优先 - 可微调本地模型全需求开地算 Ollama（本地 区）发力） 部署）

豆包1.5-pro、
- Builder 模式（自然语国内开发免费 DeepSeek Trae 字节跳动言生成项目） - 中文优者、全栈（国内 R1/V3、GPT-4o 化 - 腾讯/阿里云集成AI 开发版） （国际版）

免费试
- 低代码 + AI 生成 - 全快速应用 码上飞团自研模型（低用（企 CodeFlying 流程自动化 - 适配微信开发、初 队代码优化）业版收 小程序/H5创团队 费）